<form method="post" action="{{ path('app_einkauf_delete', {'id': einkauf.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ einkauf.id) }}">
    <button class="btn">Delete</button>
</form>
