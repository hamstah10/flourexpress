{% extends 'base.html.twig' %}

{% block title %}Sorte{% endblock %}

{% block body %}
{% block content %}
<div class="container mt-5">
<div class="row">
    <div class="col-md-12">
    <div class="card shadow">
					<div class="card-header d-flex justify-content-between align-items-center">
						<h3 class="card-title">Einkauf</h3>
                    </div>
					<div class="card-body">
{% endblock %}
    <h1>Sorte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sorte.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sorte.name }}</td>
            </tr>
            <tr>
                <th>Einkaufspreis_pro_kg</th>
                <td>{{ sorte.einkaufspreisProKg }}</td>
            </tr>
            <tr>
                <th>Verkaufspreis_1g</th>
                <td>{{ sorte.verkaufspreis1g }}</td>
            </tr>
            <tr>
                <th>Verkaufspreis_05g</th>
                <td>{{ sorte.verkaufspreis05g }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sorte_index') }}">back to list</a>

    <a href="{{ path('app_sorte_edit', {'id': sorte.id}) }}">edit</a>

    {{ include('sorte/_delete_form.html.twig') }}
    {% block contentend %}
</div>
<div class="card-footer">
    <a href="{{ path('app_sorte_new') }}" class="btn btn-primary">Neue Sorte</a>
</div>
</div>
</div>
</div></div>
{% endblock %}
{% endblock %}
