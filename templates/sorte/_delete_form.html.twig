<form method="post" action="{{ path('app_sorte_delete', {'id': sorte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sorte.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
