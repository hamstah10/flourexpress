{% extends 'base.html.twig' %}

{% block title %}Treuepunkte{% endblock %}

{% block body %}
{% block content %}
<div class="container mt-5">
<div class="row">
    <div class="col-md-12">
    <div class="card shadow">
					<div class="card-header d-flex justify-content-between align-items-center">
						<h3 class="card-title">Einkauf</h3>
                    </div>
					<div class="card-body">
{% endblock %}
    <h1>Treuepunkte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ treuepunkte.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ treuepunkte.name }}</td>
            </tr>
            <tr>
                <th>Datum</th>
                <td>{{ treuepunkte.datum ? treuepunkte.datum|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Menge</th>
                <td>{{ treuepunkte.menge }}</td>
            </tr>
            <tr>
                <th>Erledigt</th>
                <td>{{ treuepunkte.erledigt ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_treuepunkte_index') }}">back to list</a>

    <a href="{{ path('app_treuepunkte_edit', {'id': treuepunkte.id}) }}">edit</a>

    {{ include('treuepunkte/_delete_form.html.twig') }}
    {% block contentend %}
</div>
<div class="card-footer">
    <a href="{{ path('app_sorte_new') }}" class="btn btn-primary">Neue Sorte</a>
</div>
</div>
</div>
</div></div>
{% endblock %}
{% endblock %}
