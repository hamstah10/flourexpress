{% extends 'base.html.twig' %}

{% block title %}Fahrer{% endblock %}

{% block body %}
{% block content %}
<div class="container mt-5">
<div class="row">
    <div class="col-md-12">
    <div class="card shadow">
					<div class="card-header d-flex justify-content-between align-items-center">
						<h3 class="card-title">Einkauf</h3>
                    </div>
					<div class="card-body">
{% endblock %}
    <h1>Fahrer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fahrer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fahrer.name }}</td>
            </tr>
            <tr>
                <th>Verguetung1g</th>
                <td>{{ fahrer.verguetung1g }}</td>
            </tr>
            <tr>
                <th>Verguetung05g</th>
                <td>{{ fahrer.verguetung05g }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fahrer_index') }}">back to list</a>

    <a href="{{ path('app_fahrer_edit', {'id': fahrer.id}) }}">edit</a>

    {{ include('fahrer/_delete_form.html.twig') }}
    {% block contentend %}
</div>
<div class="card-footer">
    <a href="{{ path('app_sorte_new') }}" class="btn btn-primary">Neue Sorte</a>
</div>
</div>
</div>
</div></div>
{% endblock %}
{% endblock %}
